#include "arraylist.h"

int main() {
	arraylist<int> *list = new arraylist<int>();
	int choice;
	while(true) {
		if (choice == 0) {
			list->INSERT();
		}
		else if (choice == 1) {
			list->LSHOWINFO();
		}else if
	}
	
	return 0;
}
